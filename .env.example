# ================================
# ProxPanel — Variables d'environnement
# ================================
# Copiez ce fichier en .env et remplissez les valeurs

# Base de données PostgreSQL
POSTGRES_PASSWORD=changeme_strong_password

# JWT (backend)
# Générer avec: openssl rand -base64 32
JWT_SECRET=changeme_generate_with_openssl_rand_base64_32
JWT_EXPIRES_IN=7d

# CORS (backend)
CORS_ORIGIN=http://localhost:3002

# API URL (frontend)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# URL de l'app (backend)
NEXT_PUBLIC_APP_URL=http://localhost:3002

# Stripe
# Clé secrète depuis https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
# Clé publique (pour le frontend si nécessaire)
STRIPE_PUBLISHABLE_KEY=pk_test_...
# Secret webhook depuis https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_...

# ================================
# Pterodactyl (optionnel — configuré via l'UI admin)
# ================================
# L'URL et la clé API Pterodactyl sont stockées en DB (admin → panels)
# Pas besoin de variable d'env ici.

# ================================
# Notes de configuration Stripe
# ================================
# 1. Créez vos produits dans le dashboard Stripe
# 2. Copiez les Price IDs (price_xxx) dans la colonne stripePriceId des VMPlans
#    via prisma studio: docker-compose exec app npx prisma studio
# 3. Configurez le webhook pour écouter ces événements:
#    - customer.subscription.created
#    - customer.subscription.updated
#    - customer.subscription.deleted
#    - invoice.paid
#    - invoice.payment_failed
